// deprecated: NextAuth configuration removed in favor of Supabase auth helpers.
// This stub remains only to avoid breaking imports while the new auth flow settles.
export const NEXT_AUTH_DEPRECATED = true;
